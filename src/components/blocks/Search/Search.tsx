import React from 'react';
import './Search.scss';

const Search = () => (
  <form className="search-form">
    <input type="text" className="search-form__input" placeholder="Type to search..." />
  </form>
);

export default Search;
